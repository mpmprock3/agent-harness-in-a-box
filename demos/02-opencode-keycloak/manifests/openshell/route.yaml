apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: openshell-gw
  annotations:
    haproxy.router.openshift.io/enable-h2c: "true"
spec:
  tls:
    termination: passthrough
  to:
    kind: Service
    name: openshell
  port:
    targetPort: grpc
